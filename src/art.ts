import environmentPluginLogo from './assets/environment-plugin-logo.webp'

export const ENVIRONMENT_PLUGIN_LOGO =
  typeof environmentPluginLogo === 'string' ? environmentPluginLogo : environmentPluginLogo.src
